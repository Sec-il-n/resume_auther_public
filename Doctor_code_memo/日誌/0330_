・JS
テンプレートリテラル

`${定数}`

条件式
if(){

}

a === b
a !== b

func.length   引数の個数
func.arguments　引数の配列

コールバック関数
関数を関数の引数として渡す


要理解
// { } 省略でオブジェクトを返却する場合は () でくくる
(arg1, arg2) => ({"x": arg1, "y": arg2})

// デフォルト引数をサポート
(file, mode = "r") => { ... }


// 一般的な書き方
(arg1, arg2) => { return arg1 + arg2; }

// { } を省略すると return 文とみなされる
(arg1, arg2) => arg1 + arg2

// 引数が1個の場合は ( ) を省略可能
arg1 => { return arg1 * 2; }

// 引数が0個の場合は ( ) が必要
() => { return 10; }

// 可変引数をサポート
(x, y, z, ...rest) => { ... }
